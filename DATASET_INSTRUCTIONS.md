# INSTRUÇÕES PARA DATASET

A estrutura necessária no Google Drive para os Dataset como está no notebook está abaixo: Ajuste os caminhos no notebook para sua estrutura.<br>
Lembrando que o Dataset é privado e não está incluso aqui, mas pode ser incluído seu próprio Dataset seguindo a mesma estrutura.

O arquivo `images_dataset.zip` deve conter as imagens organizadas em pastas por classe, como no exemplo abaixo:
```markdown
MyDrive/
└── IA_CD_UFES/
    └── TCC/
        ├── Dataset/
        │   └── images_dataset.zip
        │        └── [person_id]/
        │            ├── front-facing/
        │            │   ├── *.jpeg
        │            │   └── ...
        │            ├── monk_scale_value.json
        │            └── ...
        └── images/
            └── calibrate_refer_data.json
```


